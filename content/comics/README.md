Only adding this for now so that I can commit the creation of the directory.

The plan is that comics will go in this directory, rather than uploading each one twice in content/en/comics and content/mt/komiks. The comics can then be fetched in the same way regardless of which language the website is accessed in.
